import styled from 'styled-components';

export const RequestButton = styled.button({
  width: '100%',
  maxWidth: '480px',
  height: '53px',
  padding: '6px 31px',
  borderRadius: '50px', 
  background: '#000', 
  color: '#fff',
  '&:hover': { cursor: 'pointer' },
});